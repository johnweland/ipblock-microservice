//import { queryDynamoDB, updateDynamoDB } from "../dynamo_handler";

import { expect } from "chai";

// TODO: flesh out test
describe("Testing queryDynamoDB()", () => {
  it("pass: return true", async () => {
    let hit = true;
    expect(hit).to.be.true;
  });
});

// TODO: flesh out test
describe("Testing updateDynamoDB()", () => {
    it("pass: the return true", async () => {
      let success = true;
      expect(success).to.be.true;
    });
  });
  